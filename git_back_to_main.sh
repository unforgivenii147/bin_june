git switch -c temp-branch    # Save current work to a new branch
git switch main              # Switch to main
git merge temp-branch        # Merge your work into main
git branch -d temp-branch    # Delete temp branch
